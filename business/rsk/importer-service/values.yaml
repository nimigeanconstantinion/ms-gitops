replicas: 1

image:
  repository: ion21/importer-service
  tag: c127a0c
  pullPolicy: IfNotPresent

containerPort: 8082

podAnnotations:
  co.elastic.logs/json.keys_under_root: "true"
  co.elastic.logs/json.add_error_key: "true"
  co.elastic.logs/json.message_key: "message"
  co.elastic.logs/json.overwrite_keys: "true"

env:
  SPRING_PROFILES_ACTIVE: argo
  KAFKA_BOOTSTRAP_SERVERS: demo-kafka-bootstrap.messaging.svc:9092
  APP_KAFKA_TOPIC: product-topic
  MYSQL_URL: "jdbc:mysql://78.96.25.131:3306/test_db"
  KEYCLOAK_SERVER_URL: https://auth.icode.mywire.org
  KEYCLOAK_REALM: rsk
  KEYCLOAK_ISSUER: https://auth.icode.mywire.org/realms/rsk
  KEYCLOAK_JWK_SET_URI: https://auth.icode.mywire.org/realms/rsk/protocol/openid-connect/certs
  SWAGGER_CLIENT_ID: importer-service-swagger

secretEnv:
  MYSQL_USERNAME: { secret: external-db, key: READ_USER }
  MYSQL_PASSWORD: { secret: external-db, key: READ_PASSWORD }

probes:
  path: /actuator/health

resources:
  requests:
    cpu: 20m
    memory: 128Mi
  limits:
    memory: 512Mi

service:
  port: 8082

ingress:
  enabled: false
