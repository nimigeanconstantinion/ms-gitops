apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: orders
  namespace: messaging
  labels:
    strimzi.io/cluster: demo
spec:
  partitions: 3
  replicas: 1
  config:
    retention.ms: 604800000   # 7 zile