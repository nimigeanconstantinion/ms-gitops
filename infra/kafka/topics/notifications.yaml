apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: notifications
  namespace: messaging
  labels:
    strimzi.io/cluster: demo
spec:
  partitions: 3
  replicas: 1
  config:
    retention.ms: 259200000   # 3 zile
